import React from 'react';

import FormControl from '@components/form-control';

const Select = () => {
  return (
    <FormControl>
      <div>Select</div>
    </FormControl>
  );
};

export default Select;
